/**
 * @typedef Events
 * @property {string} LOADED - The event was fired after provided script is loaded.
 */
const Events = Object.freeze({
  LOADED: 'ima.plugin.style.loader.loaded'
});

export default Events;
